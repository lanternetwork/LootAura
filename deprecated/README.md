# Deprecated Components

This directory contains deprecated components and utilities that are no longer part of the active codebase.

## Contents

- **`components/SalesMap.tsx`** - Legacy map component (replaced by newer implementation)
- **`components/SalesMapClustered.tsx`** - Legacy clustered map component (replaced by newer implementation)
- **`lib/clustering.ts`** - Legacy clustering utilities (replaced by newer implementation)
- **`tests/`** - Legacy test files for deprecated map components

## Status

These components are **not imported or used** anywhere in the active codebase. They are kept here for historical reference only.

## Removal

These files can be safely deleted once confirmed that:
1. No external references exist
2. No documentation references them
3. Historical reference is no longer needed

## Last Updated

December 2024 - Components marked as deprecated and moved to this directory.

