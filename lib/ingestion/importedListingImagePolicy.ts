/**
 * Shared cap for imported external listing photos (parser → publish → reconciliation).
 * Hotlink URLs only; no Cloudinary ownership for this path.
 */
export const MAX_IMPORTED_LISTING_IMAGES = 10 as const
