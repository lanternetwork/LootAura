import { describe, it, expect, test } from 'vitest'

describe('ReviewsSection', () => {
  it('smoke', () => {
    expect(true).toBe(true)
  })
  test('still runs', () => {
    expect(1).toBe(1)
  })
})